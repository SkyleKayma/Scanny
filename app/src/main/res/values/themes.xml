<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash Screen Theme -->
    <style name="Theme.Scanny.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/sc_black</item>
        <item name="postSplashScreenTheme">@style/Theme.Scanny</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.Scanny" parent="android:Theme.Material.NoActionBar" />
</resources>